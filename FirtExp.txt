 11571112361-jvd15qn2a447pdco3obkfmbcuf0p6kv9.apps.googleusercontent.com 
 
  JtT3RxQHbu6nP_DrVr45SPH- 
  sdafasdfsdf-----sdfsadf
  
  npm install --save auth0-js
  npm install firebase angularfire2 --save
  npm install firebase --save
  npm install bootstrap --save
  npm install --save jquery
  
  
  npm install -g firebase-tools  ->This will provide you with the globally accessible firebase command.
  
  
  
  
  Firebase CLI intall
  
  npm install -g firebase-tools
  
  npm install firebase-functions@latest --save
npm install -g firebase-tools
___________________________________________________________


firebase deploy --only functions:sendWelcomeEmail

ng build --prod  / ng build -prod --aot=false                   dist

firebase login --reauth
firebase init
firebase deploy

________________________________________________________________
Github commands

git init
git add -A
git status
git commit -m 'first commit' -a   --------------->m for message, commit into local rpsy
git push -u origin master

git remote -v
git remote add origin https://github.com/amvinoth/MySample.git --> add it to remote rspy
git remote -v

git push -u origin mastergi

----------Host commands
ng build                   ---------> compile pure js files



